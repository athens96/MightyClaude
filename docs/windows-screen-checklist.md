# Windows 기기 화면 점검 체크리스트

단계별로 Windows 기기에서 직접 확인해야 할 항목. macOS를 기준으로 동작을 대조한다.
각 항목은 CI 스모크 테스트가 검증할 수 없는 시각·조작적 확인이다.

---

## 스모크 테스트 (모든 빌드 공통)

빌드마다 `--smoke-test` 플래그로 WinUI를 기동하고 `smoke-result.json`을 확인한다.
스크린샷이 `artifacts/smoke/*.png`에 저장되면 아래를 눈으로 점검한다.

- [ ] 앱이 1440×920 창으로 열린다
- [ ] 좌측 사이드바에 MightyClaude 로고와 제목이 표시된다
- [ ] "워크스페이스 검색" 입력란이 있다
- [ ] "+ 프로젝트 폴더" 버튼이 있다
- [ ] "원격 연결", "설정" 버튼이 하단에 있다
- [ ] 레이아웃 드롭다운(격자/나란히/집중/탭/사용자 배치)이 있다
- [ ] 하단 상태 텍스트가 비어 있거나 정상 메시지다(오렌지 오류 텍스트 없음)

---

## 이미 있음 — 출시 전 회귀 점검

아래 기능은 현재 구현된 상태이므로 각 릴리스에서 회귀하지 않는지 확인한다.

### 실행 창 레이아웃
- [ ] 격자 레이아웃: 창이 격자로 배치된다
- [ ] 나란히 레이아웃: 창이 좌우로 배치된다
- [ ] 집중 레이아웃: 창 하나만 표시된다
- [ ] 탭 레이아웃: 탭 방식으로 전환된다

### 에이전트 필기록
- [ ] 실행 중 모델 출력이 순서대로 표시된다
- [ ] 코드 블록이 고정폭 글꼴로 렌더링된다

### 파일 첨부
- [ ] 파일 선택 후 첨부 목록에 파일명이 나타난다
- [ ] 첨부 파일이 실행 요청에 포함된다

### 세션 컨텍스트 사용량
- [ ] 컨텍스트 버튼 또는 표시 영역이 보인다
- [ ] 사용 비율이 실시간으로 갱신된다

### 권한 모드
- [ ] 실행 설정에서 권한 모드 선택이 가능하다
- [ ] 자동 허용 모드에서 도구 호출이 승인 없이 진행된다

### 원격 워크스페이스
- [ ] "원격 연결" 버튼을 누르면 연결 UI가 열린다
- [ ] 원격 서버 주소를 입력하면 연결을 시도한다

### IME 조합 처리
- [ ] 한글 IME를 사용할 때 조합 중 Enter가 실행을 트리거하지 않는다
- [ ] 조합 완료 후 Enter가 정상 전송된다

---

## 1단계 점검

각 기능을 구현한 뒤 Windows 기기에서 확인한다.

### 슬래시 명령 완성

<!-- 기기 미확인 -->

- [ ] 입력창에 `/`를 입력하면 팔레트가 열린다 (기기 미확인)
- [ ] 입력을 이어 쓰면 목록이 필터링된다 (기기 미확인)
- [ ] 항목을 선택하면 명령이 입력창에 삽입된다 (기기 미확인)
- [ ] `Esc`를 누르면 팔레트가 닫히고 초안은 그대로 남는다 (기기 미확인)
- [ ] 각 줄이 `/이름`(고정폭 반굵게)·설명(없으면 `설명 없음`)·출처 배지 순서로 보인다 (기기 미확인)
- [ ] 앱에서 바로 실행되는 명령에는 `↵`, 인수를 이어 고르는 명령에는 `›` 표식과 툴팁이 보인다 (기기 미확인)
- [ ] 푸터에 `↑↓ 이동 / Enter · Tab 선택 / Esc 닫기`와 `{개수}개`가 보인다 (기기 미확인)
- [ ] 목록이 8줄을 넘으면 스크롤되고 강조된 줄이 화면 안으로 따라온다 (기기 미확인)
- [ ] `↑`·`↓`가 강조를 옮기고 처음·끝에서 반대쪽으로 넘어간다 (기기 미확인)
- [ ] 마우스를 올리면 그 줄이 강조되고, 클릭하면 선택된다 (기기 미확인)
- [ ] 팔레트가 열려 있을 때 `Enter`·`Tab`이 요청을 보내지 않고 선택만 한다 (기기 미확인)
- [ ] `/model`·`/permissions`를 고르면 모델·권한 선택이 이어서 열린다 (기기 미확인)
- [ ] 앱 명령(`/clear`·`/cost`·`/config`·`/rename`·`/help`)은 실행되고 초안이 비워진다 (기기 미확인)
- [ ] 팔레트가 닫혀 있으면 한글 조합과 `Shift+Enter`가 이전과 똑같이 동작한다 (기기 미확인)

### 상태 표시줄

<!-- 기기 미확인 -->

- [ ] 창 하단 또는 상단에 상태 표시줄이 있다 (기기 미확인)
- [ ] 실행 중 상태(예: "실행 중…")가 표시된다 (기기 미확인)
- [ ] 실행 완료 후 상태가 갱신된다 (기기 미확인)

#### Claude 상태 줄 (`statusLine`)
- [ ] `~/.claude/settings.json`의 `statusLine` 출력이 입력창 바로 아래에 그려진다 (기기 미확인)
- [ ] 명령이 낸 색과 굵기가 그대로 보이고, 7줄 이상 출력해도 6줄까지만 그려진다 (기기 미확인)
- [ ] 저장소의 `.claude/settings.json`에 `statusLine`이 있으면 먼저 "…에 statusLine 명령이 있습니다. 이 워크스페이스에서 실행할까요?" 질문과 두 버튼이 보인다 (기기 미확인)
- [ ] "이 워크스페이스에서 허용"을 누르기 전에는 그 명령이 실행되지 않는다 (기기 미확인)
- [ ] 저장소가 명령을 바꾸면 다시 묻는다 (기기 미확인)
- [ ] 멈춘 명령(예: `sleep 60`)을 넣어도 8초 뒤 정리되고 창이 멈추지 않는다 (기기 미확인)

### CLI 계정 전환
- [ ] 설정 화면에서 연결된 CLI 계정 목록이 표시된다
- [ ] 계정을 선택하면 활성 계정이 전환된다
- [ ] 토큰·자격증명은 화면에 노출되지 않고 레이블만 표시된다
- [ ] 로그아웃 후 계정이 목록에서 제거된다

### 계정 사용량 표시
- [ ] 계정별 사용량이 기본 숨김 상태로 설정에 있다
- [ ] 사용량 표시를 켜면 수치가 나타난다

### CLI 업데이트
- [ ] 설정 화면에 CLI 업데이트 섹션이 있다
- [ ] 업데이트 확인 버튼을 누르면 확인 중 상태가 표시된다
- [ ] 업데이트 가능 시 버전 정보와 업데이트 버튼이 표시된다

### 앱 자체 업데이트
- [ ] 설정 화면에 앱 업데이트 섹션이 있다
- [ ] 최신 버전 확인이 `latest.json`의 `windows.x64` 또는 `windows.arm64` 블록을 참조한다
- [ ] 업데이트 가능 시 버전 정보와 다운로드·설치 안내가 표시된다

### 세션·창 이름 변경
- [ ] 세션 항목을 길게 누르거나 컨텍스트 메뉴에서 이름 변경이 가능하다
- [ ] 이름 입력 후 확인하면 사이드바 목록에 반영된다
- [ ] 실행 창도 동일한 방식으로 이름을 바꿀 수 있다

### Claude 플러그인 목록
- [ ] 설정 화면에 플러그인 섹션이 있다
- [ ] 감지된 Claude 플러그인이 목록에 표시된다
- [ ] 플러그인 활성화·비활성화가 가능하다

### Codex 플러그인
- [ ] (기기 미확인) Codex 플러그인이 설치된 경우 플러그인 목록에 나타난다
- [ ] (기기 미확인) Codex 실행 시 플러그인이 인식된다
- 자세한 항목은 아래 `Codex 플러그인 목록 (docs/windows-plugins.md)`에 있다.

### 설정 화면 (전체)
- [ ] 설정 화면이 별도 창 또는 다이얼로그로 열린다
- [ ] 계정, 업데이트, 컴포넌트, 스타일 섹션이 있다
- [ ] 각 섹션이 올바른 내용을 표시한다
- [ ] 설정 저장 후 앱을 재시작해도 설정이 유지된다

### 모델 기본값 설정

<!-- 기기 미확인 -->

**macOS 기기 확인 항목 (그래프 및 권한 모드 메뉴 포함)**

- [ ] (기기 미확인) 앱 설정에 "모델 기본값" 섹션이 열리고 Claude·Codex 제공자별 탭이 보인다
- [ ] (기기 미확인) 각 권한 모드 행(manual/plan/acceptEdits/auto/fullAccess)에서 기본 모델을 선택할 수 있고, Codex 탭에는 auto 행이 없다
- [ ] (기기 미확인) 모델 선택 목록이 카탈로그 이름과 직접 등록한 이름을 함께 보여 준다
- [ ] (기기 미확인) 직접 등록 이름 입력 시 앞뒤 공백이 제거되고, 빈 이름·'default'·중복·허용 문자 초과 시 등록이 거절되고 이유가 화면에 보인다 (조용히 고치지 않는다)
- [ ] (기기 미확인) 등록 이름을 지우면 그 이름을 쓰던 모드 행이 'default'로 되돌아가고 바뀐 행 수가 안내된다
- [ ] (기기 미확인) 권한 모드 메뉴 버튼에 해당 모드의 기본 모델 이름이 함께 표시된다
- [ ] (기기 미확인) 실행 그래프의 요청 노드에 실제 사용 모델 이름이 표시되고, CLI가 모델을 알리지 않으면 설정 이름 + "· 설정" 표시가 나타난다
- [ ] (기기 미확인) 잘못된 모델 이름을 CLI가 거부할 때 CLI 오류 메시지가 그대로 화면에 표시되고 다른 모델로 조용히 대체되지 않는다

**Windows 기기 확인 항목 (설정 화면·해석 규칙)**

- [ ] (기기 미확인) Windows 앱 설정에 "모델 기본값" 섹션이 열리고 Claude·Codex 제공자별 권한 모드별 기본 모델을 설정할 수 있다
- [ ] (기기 미확인) 동일한 등록 검증 규칙(빈 이름·'default'·중복·허용 문자)이 적용된다
- [ ] (기기 미확인) 모델 등록 행에 effort 지원 체크박스(CheckBox 또는 ToggleSwitch)가 있고, 켜면 제공자의 알려진 effort 수준 목록이 나타난다
- [ ] (기기 미확인) effort 지원이 켜진 상태에서 수준을 하나도 고르지 않고 등록하면 오류 메시지가 표시된다
- [ ] (기기 미확인) effort 지원을 끄고 등록하면 수준 목록 없이 저장된다
- [ ] (기기 미확인) 잘못된 이름을 설정하고 실행하면 CLI 오류 메시지가 그대로 보인다
- [ ] (기기 미확인) 권한 모드 메뉴의 각 항목에 해당 모드의 기본 모델 이름이 "· 모델" 형식으로 함께 표시되고, 기본값이 'default'이면 추가 텍스트 없이 모드 이름만 보인다
- [ ] (기기 미확인) effort 지원이 켜진 직접 등록 이름으로 실행 창 모델을 설정하면 추론 강도 메뉴에 저장된 수준 항목만 표시된다

### 단계별 모델 (`docs/phase-model-routing.md`, 시드 `seed_windows_b2_s2_phase_models`)

<!-- 기기 미확인 -->

**Windows 기기 확인 항목 (한국어)**

- [ ] (기기 미확인) 설정에 "페이즈별 모델" 칸이 "화면" 다음, "CLI 업데이트" 앞에 보인다
- [ ] (기기 미확인) 계획·실행·검토·서브에이전트 네 줄이 Claude·Codex 자세히 줄 위에 있고, 날 키(`settings.phaseModels.…`)가 하나도 보이지 않는다
- [ ] (기기 미확인) omc가 설치되어 있으면 omc 묶음에 에이전트 이름과 "(기본: opus)" 같은 설치본 기본 모델이 보이고, 설치되어 있지 않으면 묶음이 없다
- [ ] (기기 미확인) `%USERPROFILE%\.ouroboros\config.yaml`이 있으면 Ouroboros 묶음에 `*_model` 키가 보이고, 없으면 묶음이 없다
- [ ] (기기 미확인) 계획 = Opus를 고르면 `%USERPROFILE%\.config\claude-omc\config.jsonc`의 planner·architect·critic과 `config.yaml`의 `clarification.default_model`이 바뀌고, 다른 에이전트는 config.jsonc에 새로 생기지 않는다
- [ ] (기기 미확인) 쓴 뒤에도 `config.yaml`의 `orchestrator.cli_path`가 그대로이고, 옆에 `config.mighty-backup-…` 백업이 생긴다
- [ ] (기기 미확인) 매인 값이 서로 다른 줄은 "혼합"으로 보인다
- [ ] (기기 미확인) `config.jsonc`를 일부러 깨뜨리면 붉은 오류 문장("설정 파일을 읽을 수 없어 아무것도 쓰지 않았습니다: …")이 보이고 파일은 그대로다

**Windows 기기 확인 항목 (English)**

- [ ] (not checked on device) With English selected, the section reads "Phase Models" with Planning / Execution / Review / Subagents rows and no raw keys
- [ ] (not checked on device) omc agent rows show the install default as "(default: opus)"; tool headings read omc and Ouroboros
- [ ] (not checked on device) A broken `config.jsonc` shows the English file error sentence and the file stays unchanged

---

### Claude 추가 권한 요청의 앱 내 승인
- [ ] 자동 허용 밖의 도구를 Claude가 요청하면 실행 창에 승인 막대가 뜨고, 요청한 도구 이름과 입력이 전부 보인다
- [ ] 허용·거부를 누르면 실행이 그 결정대로 이어지고, 같은 요청이 두 번 뜨지 않는다
- [ ] 원격 세션과 Codex·Gemini 실행에서는 승인 막대가 뜨지 않는다(macOS와 같은 범위)
- [ ] (기기 미확인) 승인 막대에 도구 제목·요약·이유·접근 경로가 보이고, 여러 개가 기다리면 `N개 대기`가 보인다
- [ ] (기기 미확인) `이번만 허용`은 그 호출 하나만 통과시키고 다음 요청은 다시 묻는다
- [ ] (기기 미확인) 설문형 요청(`AskUserQuestion`)은 허용 버튼 없이 `거부`만 보인다
- [ ] (기기 미확인) 실행을 중지하면 기다리던 승인 막대가 모두 사라진다

### 세션·창 이름 변경
- [ ] (기기 미확인) 워크스페이스와 사이드바 실행 창을 오른쪽 클릭하면 `이름 변경…`이 보이고, 실행 창 `···` 메뉴와 탭 메뉴에도 같은 항목이 있다
- [ ] (기기 미확인) 대화상자가 현재 이름을 선택된 채로 보여주고, `이름` 레이블과 macOS와 같은 안내 문장이 보인다
- [ ] (기기 미확인) 빈 이름이면 `저장`이 꺼지고, 121자면 `이름은 120자 이내로 입력하세요.`, 줄바꿈이 있으면 `이름은 줄바꿈 없이 입력하세요.`가 빨간색으로 보인다
- [ ] (기기 미확인) Enter가 저장하고 Esc가 취소하며, 취소하면 이름이 그대로다
- [ ] (기기 미확인) 저장한 한글 이름이 탭·사이드바·창에 모두 보이고, 새 출력이 와도 앱을 껐다 켜도 그대로다
- [ ] (기기 미확인) 워크스페이스 이름을 바꿔도 폴더 경로는 그대로다

### Claude 플러그인 목록 (`docs/windows-plugins.md`)
- [ ] (기기 미확인) 실행 창 `···` 메뉴의 `Claude 플러그인`과 입력창의 `/plugin`이 같은 창을 연다
- [ ] (기기 미확인) 창 제목이 `Claude 플러그인`이고, 그 아래 워크스페이스 이름·경로와 CLI 버전이 보인다
- [ ] (기기 미확인) 실제로 설치한 플러그인이 `설치됨` 탭에 보이고, 행에 `{마켓플레이스} · {범위 이름}`과 `활성`/`비활성`이 보인다
- [ ] (기기 미확인) `마켓플레이스` 탭에 등록된 마켓플레이스의 플러그인이 `{마켓플레이스} · {소스 종류}`와 함께 보인다
- [ ] (기기 미확인) 마켓플레이스 필터의 첫 항목이 `전체`이고, 고르면 그 마켓플레이스만 남는다
- [ ] (기기 미확인) 검색어를 넣으면 이름·설명으로 걸러지고, 없으면 `검색 조건에 맞는 플러그인이 없습니다.`가 보인다
- [ ] (기기 미확인) `목록 새로고침`이 목록을 다시 읽고, 읽는 동안 `목록을 불러오는 중…`이 보인다
- [ ] (기기 미확인) 창에 설치 단추·설치 범위 선택기·마켓플레이스 새로고침이 **없고**, 창을 열고 닫아도 `claude plugin list`가 설치 상태를 바꾸지 않는다
- [ ] (기기 미확인) Claude CLI를 PATH에서 치우면 `Claude CLI가 설치되어 있지 않습니다. 먼저 CLI를 설치하세요.`가 보인다
- [ ] (기기 미확인) 2.1.268 미만 CLI에서는 `… Claude Code 2.1.268 이상이 필요합니다.`가 보인다
- [ ] (기기 미확인) 등록된 마켓플레이스가 하나도 없으면 `… marketplace add로 등록한 뒤 …` 문장과 `마켓플레이스 추가 방법` 링크가 보인다
- [ ] (기기 미확인) 원격 워크스페이스에서 열면 `원격 워크스페이스에서는 관리할 수 없습니다.`와 `원격 컴퓨터의 MightyClaude에서 플러그인을 관리하세요.` 두 문장만 보이고 목록을 읽지 않는다

### Codex 플러그인 목록 (`docs/windows-plugins.md`)
- [ ] (기기 미확인) 실행 창 `···` 메뉴의 `Codex 플러그인`과 입력창의 `/plugins`가 같은 창을 연다
- [ ] (기기 미확인) 창 제목이 `Codex 플러그인`이고, 그 아래 워크스페이스 이름·경로와 `codex --version`이 보인다
- [ ] (기기 미확인) 실제로 설치한 Codex 플러그인이 `설치됨` 탭에 보이고, 행의 범위가 언제나 `사용자 · 전체`다
- [ ] (기기 미확인) `마켓플레이스` 탭에 등록된 마켓플레이스와 큐레이티드 원격 목록의 플러그인이 `{마켓플레이스} · {소스 종류}`와 함께 보인다
- [ ] (기기 미확인) 목록 아래 안내 문장이 `이 PC의 Codex 설치 목록과 마켓플레이스 목록입니다. 설치 후 새 Codex 세션을 시작하세요.`다
- [ ] (기기 미확인) 목록을 정상으로 읽은 뒤에도 상태 문장이 보인다(Claude 창은 보이지 않는다)
- [ ] (기기 미확인) 창에 설치 단추·설치 범위 선택기·마켓플레이스 새로고침이 **없고**, 창을 열고 닫아도 `codex plugin list`가 설치 상태를 바꾸지 않는다
- [ ] (기기 미확인) Codex CLI를 PATH에서 치우면 `Codex CLI가 설치되어 있지 않습니다. 먼저 CLI를 설치하세요.`가 보인다
- [ ] (기기 미확인) 플러그인 명령이 없는 옛 Codex CLI에서는 `설치된 Codex CLI가 필요한 JSON 플러그인 명령을 지원하지 않습니다. 최신 Codex CLI로 업데이트하세요.`가 보이고 목록 명령이 실행되지 않는다
- [ ] (기기 미확인) 등록된 마켓플레이스가 하나도 없으면 `등록된 마켓플레이스가 없습니다. Codex CLI에서 plugin marketplace add로 …` 문장과 `Codex CLI에서 마켓플레이스를 등록한 뒤 목록을 새로고침하세요.`가 보인다(Claude의 `마켓플레이스 추가 방법` 링크는 보이지 않는다)
- [ ] (기기 미확인) 원격 워크스페이스에서 열면 macOS의 두 문장만 보이고 목록을 읽지 않는다

## 터미널 단계 점검

### 인터랙티브 터미널
- [ ] 실행 창과 별개로 터미널 창이 열린다
- [ ] 키보드 입력이 쉘 세션에 전달된다
- [ ] 출력이 실시간으로 스크롤된다
- [ ] 창을 닫아도 쉘 세션이 계속 실행된다(상태 유지)
- [ ] macOS의 Ghostty/PTY에 대응하는 Windows PTY 동작이다

---

## 2단계 점검

### 스타일 엔진
- [ ] 설정 화면에 스타일 섹션이 있다
- [ ] 스타일 매니페스트를 불러오면 항목이 표시된다
- [ ] 스타일 승인 시트가 열리고 승인·거부가 가능하다
- [ ] 승인된 스타일이 실행 창에 적용된다

### 가이드 패널
- [ ] 실행 중 에이전트 질문이 별도 패널에 표시된다
- [ ] 답변 입력 후 에이전트가 계속 진행한다

### 실행 그래프
- [ ] 실행 그래프 뷰가 열린다
- [ ] 노드와 엣지가 렌더링된다
- [ ] 노드 선택 시 세부 정보가 표시된다

### 컴포넌트 (설치 대시보드) (`docs/toolkit.md`, 시드 `seed_windows_b2_s3_components_toolkit`)

<!-- 기기 미확인 -->

**Windows 기기 확인 항목 (한국어)** — x64 PC, CI ZIP

- [ ] (기기 미확인) 설정 → "구성 요소"에 Claude·Codex·Gemini CLI 줄과 "내 작업 도구 모음" 목록이 보이고, 날 키(`settings.components.…`, `settings.toolkit.…`)가 하나도 보이지 않는다
- [ ] (기기 미확인) 설치되지 않은 CLI 줄은 상태가 "설치 필요"이고, 그 줄의 "설치 필요" 단추를 누르면 `npm install -g …` 설치 명령이 클립보드에 복사된다
- [ ] (기기 미확인) 승인한 winget 항목이 "설치"를 누르면 확인 창에 `winget install --exact --id … --scope user …` argv 그대로 보이고, 확인하면 설치된 뒤 결과 표에 "설치됨"이 보인다
- [ ] (기기 미확인) Mac에서 추가한 brew 항목이 든 `toolkit.json`(또는 Mac 내보내기 파일 가져오기)은 목록에 brew 항목을 보이지 않지만, 승인·삭제·저장 뒤에도 `%APPDATA%\MightyClaudeNative\toolkit.json`에 그 항목이 그대로 남는다

**Windows 기기 확인 항목 (English)**

- [ ] (not checked on device) With English selected, Settings → Components shows the Claude / Codex / Gemini CLI rows and the toolkit list with no raw keys
- [ ] (not checked on device) A missing CLI row reads "Install required", and its "Install required" button copies the `npm install -g …` install command
- [ ] (not checked on device) An approved winget entry installs after the confirm view listing its argv and then shows "Installed" in the result table
- [ ] (not checked on device) A brew entry added on the Mac stays in `toolkit.json` but is not listed on Windows

### 워크스페이스 Git 정보
- [ ] Git 저장소 워크스페이스에서 현재 브랜치와 상태가 표시된다

### 컨텍스트 압축 표시
- [ ] 컨텍스트 압축 이벤트 발생 시 UI에 표시된다

### 그래프 블록 크기 조정
- [ ] 실행 그래프에서 블록 크기를 조정할 수 있다

### 참조 링크 버블
- [ ] 실행 그래프에서 참조 링크가 버블로 표시된다
- [ ] 버블을 클릭하면 링크가 열린다

### 세션 템플릿
- [ ] 세션을 템플릿으로 저장할 수 있다
- [ ] 저장된 템플릿으로 새 세션을 시작할 수 있다

---

### 플러그인 마켓플레이스 (Claude·Codex) — 기기 미확인
- [ ] 마켓플레이스 탭에 설치 단추가 플러그인마다 보이고, 이미 같은 범위에 설치된 플러그인의 단추는 비활성이다 (기기 미확인)
- [ ] Claude 범위 선택기에 로컬·프로젝트·사용자 세 항목이 macOS와 같은 문구로 보인다 (기기 미확인)
- [ ] Codex 범위 선택기에는 사용자 항목 하나만 보인다 (기기 미확인)
- [ ] 설치 단추를 누르면 진행 레이블과 취소 단추가 보이고 닫기가 비활성이 된다 (기기 미확인)
- [ ] 취소를 누르면 작업이 멈추고 취소됐다는 macOS 문장이 보인다 (기기 미확인)
- [ ] 설치 완료 뒤 결과 문장이 보이고 목록이 새로고침된다 (기기 미확인)
- [ ] 마켓플레이스 새로고침 단추가 등록된 마켓플레이스가 있을 때만 활성이다 (기기 미확인)
- [ ] 원격 워크스페이스에서는 설치·새로고침이 실행되지 않는다 (기기 미확인)

## 3단계 점검

### 동반 펫
- [ ] 펫이 화면 모서리 또는 지정 위치에 표시된다
- [ ] 펫 종류를 선택할 수 있다
- [ ] 펫이 실행 상태에 반응한다

### 모바일 릴레이 호스트
- [ ] Windows 앱이 릴레이 서버 호스트로 동작한다
- [ ] 모바일 앱에서 연결 후 실행이 가능하다

### 사용자 설문
- [ ] 초기 설정 시 설문 화면이 표시된다
- [ ] 설문 응답이 저장된다

---

### 완료 알림
- [ ] 앱이 뒤에 있을 때 실행이 끝나면 Windows 알림(토스트)이 한 번 뜨고, 누르면 해당 실행 창으로 이동한다
- [ ] 앱이 앞에 있을 때는 알림이 뜨지 않는다

### CLI 업데이트 (기기 미확인)
- [ ] winget으로 설치한 CLI가 `winget` 방식으로 표시되고, 업데이트 시 그 패키지 하나만 올라간다
- [ ] npm 전역 설치 CLI가 `npm` 방식으로 표시되고, 업데이트 시 공식 패키지만 올라간다
- [ ] 네이티브 Claude Code 설치가 `native`로 표시되고 `claude update`로 갱신된다
- [ ] 업데이트 중 권한 상승(UAC) 창이 뜨지 않는다
- [ ] 업데이트 중 취소를 누르면 설치 프로그램과 그 자식 프로세스가 모두 멈춘다
- [ ] 시험판 npm 채널이 `건너뜀`으로 표시되고 버전이 바뀌지 않는다

### 계정 사용량 표시
- [ ] 로컬 AI 실행 창이 있는 실행기마다 하단 상태 줄에 칩이 하나씩 보인다
- [ ] `shell` 실행 창만 있거나 원격 워크스페이스만 있으면 칩이 없다
- [ ] 아무것도 켜지 않은 상태에서 Claude 칩이 `실행 후 표시`로 보인다
- [ ] Claude를 한 번 실행하면 CLI가 보고한 세션·주간 한도가 칩과 팝오버에 나타난다
- [ ] Codex에 로그인되어 있으면 칩에 세션·주간 사용률이 보인다
- [ ] Gemini 카드에 `/stats`를 안내하는 문장이 보인다
- [ ] 칩을 누르면 팝오버에 창별 사용률·진행 막대·초기화 시각·마지막 확인 시각이 보인다
- [ ] 새로고침 버튼을 누르는 동안 창이 멈추지 않는다
- [ ] 직접 조회 스위치를 켜면 Claude 한도가 채워지고, 끄면 조회로 받은 값이 사라진다
- [ ] 직접 조회를 켠 채 앱을 다시 켜면 스위치가 켜진 상태로 남는다
- [ ] 네트워크를 끊고 새로고침하면 마지막 값이 `마지막 확인값 · `과 함께 남는다
- [ ] 조회 중에 창을 닫으면 앱이 바로 종료된다
- [ ] 조회 전후로 `%USERPROFILE%\.claude\.credentials.json`의 내용과 수정 시각이 그대로다

#### 리셋권 (한도 리셋 자격) — 기기 미확인

- [ ] (기기 미확인) 직접 조회 스위치가 꺼져 있을 때 리셋권 행이 팝오버에 보이지 않는다
- [ ] (기기 미확인) 직접 조회 스위치를 켜면 팝오버에 리셋권 행과 "claude.ai에서 리셋" 링크가 보인다
- [ ] (기기 미확인) "claude.ai에서 리셋" 링크는 모든 상태(unknown 포함)에서 활성이다
- [ ] (기기 미확인) 리셋권이 없는 계정에서 `이 계정에는 현재 리셋권이 제공되지 않습니다.` 문장이 보인다
- [ ] (기기 미확인) 조회 전후로 자격 증명 파일의 내용과 수정 시각이 그대로다 (앱은 POST를 보내지 않는다)

### 앱 자체 업데이트 (기기 미확인)
- [ ] 설정에 `앱 업데이트` 섹션이 `CLI 계정`과 `앱 정보` 사이에 보인다
- [ ] 공개 키 없이 만든 빌드에서 상태 문장 한 줄만 보이고 `업데이트 확인` 버튼이 비활성이다
- [ ] 공개 키 없는 빌드에서 확인 버튼을 눌러도 네트워크 요청이 나가지 않는다
- [ ] 빌드에 기본 주소가 있으면 주소 칸이 비활성이고 그 아래에 `빌드에 포함된 주소를 사용합니다: …`가 보인다
- [ ] 빌드에 기본 주소가 있을 때 주소 칸에 다른 주소를 넣어도 빌드 주소만 조회한다
- [ ] `업데이트 확인`을 누르면 `새 버전 확인 중…` → `최신 버전입니다.` 또는 `새 버전 x.y.z 이 있습니다.`로 바뀐다
- [ ] 새 버전이 있을 때 버튼이 `다운로드`로 바뀌고, 받는 동안 `{n}% 받는 중…`과 `취소`가 보인다
- [ ] `취소`를 누르면 `updates/<버전>/` 폴더가 남지 않는다
- [ ] 받기가 끝나면 `패키지를 풀고 확인하는 중…`을 거쳐 `설치 준비 완료`와 `설치하고 다시 실행`이 보인다
- [ ] `설치하고 다시 실행`을 누르면 앱이 종료되고 잠시 뒤 새 버전이 스스로 열린다
- [ ] 교체 중 권한 상승(UAC) 창이 뜨지 않는다
- [ ] 교체 뒤 `앱 정보`의 버전이 새 버전이고, 설정·대화·초안이 그대로 남아 있다
- [ ] 교체 뒤 설치 폴더 옆에 `*.backup-*` 폴더가 남아 있으면 앱을 한 번 더 껐다 켰을 때 사라진다
- [ ] 일부러 sha256을 틀리게 적은 manifest로는 다운로드가 실패하고 설치 폴더가 그대로다
- [ ] 받아 둔 zip을 손으로 바꾸고 설치를 누르면 교체되지 않고 이전 버전이 그대로 열린다
- [ ] `앱 시작 시 하루 한 번 새 버전 확인`을 끄면 다시 켤 때 확인하지 않는다
- [ ] 자동 확인이 켜져 있어도 창이 뜨는 속도가 느려지지 않는다
- [ ] arm64 기기에서 arm64 패키지를 받고, x64 패키지만 있는 manifest에서는 안내만 보인다
- [ ] x64 기기에서 한 버전에서 다음 버전으로 실제 업데이트: 도우미가 스테이지된 폴더에서 시작되고 새 버전이 설치 폴더에 복사되어 열린다 (기기 미확인)
- [ ] arm64 기기에서 한 버전에서 다음 버전으로 실제 업데이트: 같은 순서, arm64 패키지 (기기 미확인)

---

## 배포 인프라 점검

### Windows 빌드·서명·패키징
- [ ] MSIX 패키지가 생성된다
- [ ] 코드 서명이 적용되어 설치 시 경고가 없다
- [ ] winget 패키지로 `winget install MightyClaude`가 동작한다
- [ ] 이전 버전에서 업데이트 설치 시 설정이 유지된다

---

*macOS 기준: macOS 동작이 참조 구현이다. Cmd → Ctrl 단축키 대응 외에 동작이 다른 경우 이슈로 기록한다.*
