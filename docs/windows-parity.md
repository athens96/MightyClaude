# Windows 기능 대응 목록

macOS 코드베이스에서 발견한 모든 기능 영역과 Windows 구현 현황.
단계 셀은 `1단계 | 터미널 | 2단계 | 3단계 | 이미 있음 | Windows에 해당 없음 | 배포 인프라` 중 하나.
'확인 필요' 표시는 단계가 판단으로 결정된 행. '보류'는 macOS와의 차이를 사용자가 정해야 하는 행 ('확인 필요'와 다른 표시).
문구 규칙(v4부터): 설정 화면과 안내 패널의 문구는 두 앱이 같은 키로 `locales/ko.json`·`locales/en.json`을 읽는다. 아직 옮기지 않은 문구는 종전대로 macOS 리터럴과 같아야 하며, 남은 수는 `node scripts/check-locales.js`가 센다(`docs/i18n.md`).

| 기능 영역 | macOS 근거 | Windows 현황 | 단계 | 근거 | 확인 |
|---|---|---|---|---|---|
| 슬래시 명령 완성 | `SlashCommands.swift`, `SlashCommandPalette.swift`, `AppStore+SlashCommands.swift` | `SlashCommands.cs`(`SlashPalette`·`SlashPaletteState`), `SlashCommandStrings.cs`, WinUI `MainWindow.SlashPalette.cs` — 입력창 위 완성 목록·행·배지·표식·푸터·개수, ↑↓ 이동 / Enter·Tab·클릭 선택 / Esc 닫기; `SlashPaletteVerification.cs` 10개 검사 Mac 통과; 스모크 키 `slashCommandPalette`가 실제 컨트롤을 구동 | 1단계 | P1 합의 (interview_20260919_235018) | 완료 — `9b47538`(Windows x64·arm64 성공). Core `14d8613`·`6114df4`, 화면 `685b3a1`. `OpenPlugins`(`/plugin`·`/plugins`)는 플러그인 화면이 생길 때 돌아온다(`docs/windows-slash-commands.md`). 기기 미확인 |
| 상태 표시줄 | `StatusLine.swift`, `StatusLineView.swift`, `AppStore+StatusLine.swift` | `StatusLine.cs`, `StatusLineTrust.cs`, `StatusLineStrings.cs` — Core 구현 완료; `StatusLineVerification.cs` 8개 테스트 통과 (Mac); WinUI `MainWindow.StatusLine.cs` — 입력창 아래 렌더링·신뢰 질문·스모크 키 `statusLine` | 1단계 | P1 합의 (interview_20260919_235018) | 완료 — 실제 실행 창에 연결(`6f1231d`, `StatusLineRefresher`), 명령에 넘기는 JSON을 Claude Code·macOS와 같은 모양으로 맞춤(`041a809`), 256색·RGB와 사용자 수준 폴백(`ddea1de`). Core 127개 검사. 실제 statusLine 스크립트로는 기기 미확인 |
| 상태 줄 256색·RGB | `StatusLine.swift` `ANSISegment.Color.palette/.rgb` | 보류 — Windows `AnsiColor`는 macOS의 16색 이름만 지원. `38;5;n` / `38;2;r;g;b`는 기본색으로 표시됨 | 1단계 | 보류 (v4 묶음 전 사용자 판단) | 완료 — `ddea1de`. macOS와 같은 파서·xterm 팔레트 |
| 상태 줄 레벨 분리 폴백 | `AppStore+StatusLine.swift` `gated ? discovery.user : discovery.preferred` | 보류 — Windows `Discover()`는 우선순위 1개만 반환하므로, 워크스페이스 명령이 거절되면 사용자 설정 명령으로 되돌아가지 않고 상태 줄이 비어 있음 | 1단계 | 보류 (v4 묶음 전 사용자 판단) | 완료 — `ddea1de`. 워크스페이스 명령이 허락을 기다리는 동안 사용자 수준 명령을 쓴다 |
| CLI 계정 전환 | `CLIAccounts.swift`, `CLIAccountsSettingsView.swift`, `AppStore+CLIAccounts.swift` | `CliAccounts.cs`(상태·로그아웃·로그인 argv), `CliAccountStrings.cs`(문구), `CliAccountsCoordinator`(러너·파일 연결), `CliAccountSmoke`(픽스처·확인 흐름) — `cli account …` 검사 6개 Mac Core.Tests 통과; 스모크 키 `cliAccountsSection`; 섹션 등록 완료(`CliAccountsTitle`); 외부 터미널 규칙·문구 대체는 `docs/windows-cli-accounts.md` | 1단계 | P1 합의 (interview_20260919_235018) | 완료 — `11187f3`(Windows 성공). Core `b9c0da1`, 설정 섹션·실제 앱 연결 `2bcd3c9`, 토큰이 남지 않는다는 검사 `11187f3`. 로그인은 외부 터미널(문장 1개 Windows용, 확인 필요). 실제 로그인·로그아웃은 기기 미확인 |
| 계정 사용량 표시 | `AccountUsageService.swift`, `AccountUsageSnapshot.swift`, `StatusBarUsage.swift`; docs/session-usage.md | `AccountUsage.cs`(스냅샷·서비스·Claude 조회·Codex app-server), `AccountUsagePresentation.cs`(칩·팝오버 줄), `AccountUsageStrings.cs`(문구), `AccountUsageRuntime.cs`(전송) — 하단 상태 줄 칩 + 상세 팝오버(창별 사용률·초기화 시각·마지막 확인 시각·새로고침·직접 조회 스위치). 직접 조회는 기본 꺼짐이고, 꺼진 동안 Claude는 실행 중 CLI가 보고한 한도만, Codex는 자기 CLI의 app-server, Gemini는 macOS 문장. `account usage …` 검사 8개(비밀값·호스트/리디렉션 포함) Mac Core.Tests 통과; 스모크 키 `accountUsage`; 규칙은 `docs/windows-account-usage.md` | 1단계 | P1 합의 — "usage display, off by default" | 완료 — Core `d20fe71`, 상태 막대 칩·팝오버와 실제 앱 연결 `50224fd`. Claude 직접 조회는 기본 꺼짐, 켜면 `api.anthropic.com`에만 HTTPS로(리디렉션 거부, 10초), 로그인 정보는 그 요청 동안만 메모리에. Keychain이 없는 차이는 `docs/windows-account-usage.md`(확인 필요). 실제 계정으로는 기기 미확인 |
| CLI 업데이트 | `CLIUpdateService.swift`, `CLIUpdateSettingsView.swift`, `AppStore+CLIUpdates.swift` | `CliUpdateService.cs`, `CliUpdateStrings.cs`, `CliUpdateCoordinator.cs` — 설치된 CLI만 그 설치 방식(native·winget·npm)으로 업데이트, 상태 `updated`/`current`/`skipped`/`failed`/`cancelled`/`busy`, 동시 1건·취소·종료, 시작 시 자동 업데이트 게이트; `업데이트 하기` 버튼(실행 중엔 `업데이트 중…`, 비활성); 앱 시작 시 스위치 켜져 있으면 자동 실행(--smoke-test 제외); `cli update coordinator …` 검사 4개 포함 총 Mac Core.Tests 111개 통과; 스모크 키 `liveWiring`. 규칙은 `docs/windows-cli-update.md` | 1단계 | P1 합의 (interview_20260919_235018) | 완료 — `업데이트 하기` 버튼과 앱 시작 시 자동 실행 연결(`e0455c6`, `CliUpdateCoordinator`), 규칙 `45ce518`. 실제 업데이트는 기기 미확인 |
| 리셋권 (한도 리셋 자격) | `AccountUsageService.swift`·`AccountUsageSnapshot.swift`·`StatusBarUsage.swift`; docs/session-usage.md | Core: 일곱 상태(unknown·ineligible·none·exhausted·cooldown·held·available)와 두 프로그램(cedar_ember·juniper_tide)의 결정 목록, `GET /api/oauth/usage?at_wall=1&skip_spend=1`·`GET /api/oauth/usage?cedar_ember=1&skip_spend=1` GET 전용 읽기, `AccountUsageSnapshot` 엔타이틀먼트 행, `AccountUsageVerification.cs`에 per-state PASS usageReset 검사 7개; WinUI: 사용량 팝오버에 리셋권 행 + "claude.ai에서 리셋" 링크, MainWindow.Smoke.cs `usageReset` 스모크 키; 앱은 POST 없음(GET 전용, 토큰 쓰기 없음). 미루기: 휴대폰 릴레이 계약 확장, 앱 내 리셋 실행 — docs/windows-parity.md·docs/mobile-remote.md·docs/session-usage.md 기록 | 1단계 | 계정 사용량 표시(1단계) 위에 얹는 읽기 전용 자격 표시 | 완료 — Core 결정 목록·상태 행·일곱 상태 표 테스트 `9e856db`, WinUI 행·링크 `9e856db`, per-state PASS `7d294bf`, POST 가드·allow-list `b14830f`, 스모크 인프라 `066db49`·`b922583`. 실제 계정으로는 기기 미확인 |
| 계정 사용량 Keychain 문구 | `StatusBarUsage.swift`의 Keychain 문장 3개 | 보류 — Windows에는 Keychain이 없어 스위치 이름·설명과 공유 한도 안내 문장이 macOS 원문 없이 Windows 문구로 들어간다 | 1단계 | 보류 (v4 묶음 전 사용자 판단) | 확인 필요 — Keychain을 직접 조회로 바꾼 OS 이름 치환 문장 3개(`docs/windows-account-usage.md`). 보류가 아니라 문구 확인 항목 |
| CLI 업데이트 winget 문구 | `CLIUpdateService.swift`의 Homebrew 문장 2개 | 보류 — Windows에는 Homebrew가 없어 `설치된 winget의 해당 패키지만 업데이트합니다.` · `winget 설치이지만 winget 실행 파일을 찾지 못했습니다.` 두 문장이 macOS 원문 없이 들어간다 | 1단계 | 보류 (v4 묶음 전 사용자 판단) | 확인 필요 — Homebrew를 winget으로 바꾼 OS 이름 치환 문장 2개(`docs/windows-cli-update.md`). 보류가 아니라 문구 확인 항목 |
| 앱 업데이트 공개 키 없는 빌드 | `AppUpdateSettingsView.swift`의 "서명 검증 없음" 경고 | 보류 — Windows는 공개 키가 없으면 확인 자체를 하지 않으므로 macOS의 주황색 경고 문장이 설 자리가 없고, 대신 macOS 원문이 없는 `이 빌드에는 업데이트 공개 키가 없어 업데이트 확인을 지원하지 않습니다.` 한 문장이 들어간다 | 1단계 | 보류 (v4 묶음 전 사용자 판단) | 확인 필요 — 규칙 1의 화면·문구 차이 1건(`docs/windows-app-update.md`). 동작 차이는 사용자가 정한 "Windows는 더 엄격하게"에 따른 것이고, 문구만 확인 항목 |
| 앱 자체 업데이트 | `AppUpdate.swift`, `AppUpdateSettingsView.swift`, `AppStore+AppUpdate.swift` | Core: `AppUpdateManifest.cs`(서명 봉투·버전 비교·자산 규칙), `Ed25519Verify.cs`(검증 전용), `AppUpdateService.cs`(https 전용 전송·다운로드·풀기), `AppUpdateReplacement.cs`(교체 계획·재검증·되돌리기·복사 설치), `AppUpdateCoordinator.cs`(단계·표시·자동 확인), `AppUpdateStrings.cs`(문구); Tests: `AppUpdateVerification.cs`(복사·되돌리기·스테이지 도우미 3개 포함 18개), `Ed25519Fixture.cs`; WinUI: `MainWindow.AppUpdate.cs`(스테이지된 폴더의 도우미 실행); 빌드: `scripts/build-windows.ps1`(공개 키·주소 새김) — macOS와 같은 섹션·문구·버튼(업데이트 확인 / 다운로드 / 취소 / 설치하고 다시 실행 / 진행 중…). macOS보다 엄격한 네 규칙: 공개 키 없는 빌드는 확인 자체를 하지 않음, sha256·size 없는 자산 거절, 빌드 주소가 사용자 주소를 무시, 교체 직전 sha256 재검증. `app update …` 검사 18개 Mac Core.Tests 통과; 스모크 키 `appUpdateSection`; 규칙은 `docs/windows-app-update.md` | 1단계 | P1 합의 (interview_20260919_235018) | 완료 — `004c7fa`·`55f2dbf`·`d772e9a`·`b4f0d70` (Windows x64·arm64 성공). 도우미가 스테이지된 폴더에서 실행되고 설치 폴더를 이름 변경 후 복사로 교체(`AppUpdateReplacement.cs`·`MainWindow.AppUpdate.cs` 수정, `AppUpdateVerification.cs` 복사·되돌리기·스테이지 도우미 검사 추가). 기기 미확인 |
| 이름 줄바꿈 오류 문구 | `RenameViews.swift`의 `이름은 줄바꿈 없이 입력하세요.` | OS 기본 동작 차이(결정 불필요) — 한 줄짜리 WinUI `TextBox`가 `\r`·`\n`을 Text 속성에 닿기 전에 버려서, 입력란을 통해서는 이 문구가 뜰 일이 없다. 문구·규칙은 `RenameStrings.ErrorControlCharacter`·`RenameSupport`에 그대로 있고 `rename …` Core.Tests 검사가 증명하지만, 화면에서 보이지 않는다 | 1단계 | OS 고유 방식 (입력란 기본 동작) | 확인 필요(기기) — 탭처럼 줄바꿈이 아닌 제어 문자를 붙여 넣으면 문구가 그대로 뜨는지는 기기에서만 볼 수 있다. macOS에서 이 문구가 실제로 보이는 경로가 있는지 코드만으로는 정할 수 없다. Windows는 줄바꿈을 아예 들이지 않으므로 더 엄격하다. 스모크 검사는 줄바꿈이 입력란에서 살아남지 못하는 것과 저장 버튼·문구가 Core 규칙과 일치하는 것을 본다 (`docs/windows-rename.md`) |
| 세션·창 이름 변경 | `AppStore+Rename.swift`, `RenameViews.swift`, `RenameDiagnostics.swift` | Core: `RenameStrings.cs`(문구), `RenameSupport.cs`(검증·문구·이름 변경·제목 클램프), `DesktopService.RenameWorkspaceAsync`·`RenameSessionAsync`, `StateStore.cs`(제목을 텍스트 요소 120개로 클램프); Tests: `RenameVerification.cs`(`rename …` 검사 16개 — 검증 규칙·스냅샷 저장·새 출력/재시작 생존·거절·DesktopService 연결·macOS 문구 일치·대화상자 문구/저장 버튼·이모지 제목 클램프); WinUI: `MainWindow.Navigation.cs`(`WorkspaceMenu`·`SessionMenu`가 `이름 변경…` 항목을 만들고 `menu.Opening`에서 `dialogOpen`이면 끈다, `RenameDialog` 대화상자, `RenameWorkspace`·`RenameSession`), `MainWindow.cs`(워크스페이스 목록 항목과 사이드바 실행 창 단추에 그 메뉴를 `ContextFlyout`으로 달고, 실행 창 `···`의 `MoreMenu`가 같은 항목을 직접 만든다, `dialogOpen` 플래그), `MainWindow.Layout.cs`(탭의 `ContextFlyout`과 탭 더블 탭, 실행 창 본문의 `ContextFlyout`), `MainWindow.SlashPalette.cs`(`SlashCommandAction.Rename`), `MainWindow.Smoke.cs`(`RunRenameSmoke`, 스모크 키 `rename`) — macOS와 같은 문구·검증(공백 제거·빈 문자열·120자·제어 문자); 텍스트 요소 기준 120자(Korean·emoji 포함); WinUI에 새 문구 리터럴 없음; 규칙은 `docs/windows-rename.md` | 1단계 | macOS 동일 화면 원칙; P1 기본 UX | 완료 — `d099230`·`6244f1c`·`4e9e42f`·`a770c9f`·`7cf8b31`·`07292a4` (Windows x64·arm64 성공, `07292a4` 기준). `rename …` 검사 16개 포함 Core.Tests 179 통과, 스냅샷 `Version` 1 유지. Windows CI가 두 번 빨간색이었고 둘 다 공개 주석으로 원인을 읽어 고쳤다(입력 감지를 `TextBox.TextProperty` 감시로, 한 줄 입력란의 줄바꿈 제거 동작에 맞춘 스모크). 기기 미확인 |
| Claude 플러그인 목록 | `ClaudePluginService.swift`, `ClaudePluginModels.swift`, `ClaudePluginView.swift` | Core: `PluginStrings.cs`(문구), `ClaudePlugins.cs`(모델·파서·`ClaudePluginStatus`), `ClaudePluginReader.cs`(`claude --version` · `claude plugin list --json --available` · `claude plugin marketplace list --json`을 워크스페이스 폴더에서 공용 러너로 실행, 20초·8 MiB·512 KiB), `ClaudePluginPresentation.cs`(`ClaudePluginBrowser` 탭·필터·검색·행·빈 목록 문구, `ClaudePluginSmokeOutcome`); WinUI: `MainWindow.Plugins.cs`(플러그인 창), `MainWindow.cs`(실행 창 `···`의 `Claude 플러그인`), `MainWindow.SlashPalette.cs`(`/plugin`) — 보기 전용이라 설치 단추·범위 선택기·마켓플레이스 새로고침이 화면에 없다. CLI 없음·구버전·느림·형식 오류는 각각 macOS 문장을 단 상태가 되고, 원격 워크스페이스는 macOS 두 문장을 보여주며 아무것도 실행하지 않는다. `claude plugin …` 검사 12개 Mac Core.Tests 통과(총 191); 스모크 키 `claudePluginList`; 규칙은 `docs/windows-plugins.md` | 1단계 | 슬래시 명령 완성의 전제 조건 | 완료 — `b439d52`·`9fdff7b`·`91a0669`·`682de10`·`d91ce6c`·`593dd90` (Windows x64·arm64 성공, `593dd90` 기준). `claude plugin …` 검사 13개 포함 Core.Tests 통과. `/plugin`·`/plugins`가 Claude 팔레트에 돌아왔다. 확인 필요 — OS 이름 치환 문장 1개(`이 Mac의` → `이 PC의`). 기기 미확인 |
| 플러그인 목록 동시 요청 | `ClaudePluginService.swift`의 `busy` 상태와 `다른 플러그인 작업이 진행 중입니다.` | 보류 — Windows는 진행 중인 읽기에 합류해 같은 답을 돌려주므로 macOS의 `busy` 문장이 화면에 뜰 일이 없다 | 1단계 | 보류 (v4 묶음 전 사용자 판단) | 확인 필요 — 시드가 정한 방식(`한 번에 하나, 두 번째 요청은 진행 중인 것으로 답함`). 보기만 하는 읽기라 거절할 이유가 없다는 판단이고, 보이는 동작 차이라 보류 행으로 남긴다 (`docs/windows-plugins.md`) |
| 플러그인 설치·마켓플레이스 등록 | `ClaudePluginService.install`, `refreshMarketplace`, `ClaudePluginView.swift`의 설치 단추·범위 선택기 | 보류 — 이 기능은 보기 전용이라 설치 단추, 설치 범위 선택기(`로컬 · 이 워크스페이스, 나만` / `프로젝트 · 팀과 공유` / `사용자 · 모든 프로젝트`), `마켓플레이스 새로고침`이 화면에 없다 | 1단계 | 보류 — 마켓플레이스 기능 묶음 | 해소 — 플러그인 마켓플레이스 행(`28b41e6`·`1f2563f`·`21c8876`)에서 설치 단추·범위 선택기·마켓플레이스 새로고침이 들어왔다 |
| Codex 플러그인 | `CodexPluginService.swift` | Core 새 파일: `CodexPluginStrings.cs`(Codex만 다르게 말하는 문구, 나머지는 `PluginStrings.cs` 재사용), `CodexPluginReader.cs`(`codex --version` 4초 → `--help` capability probe 4개 4초 → `codex plugin list --json --available` 20초·8 MiB → `codex plugin marketplace list --json` 20초·512 KiB). 고친 Core 파일: `ClaudePlugins.cs`(`ParseCodexSnapshot`), `ClaudePluginPresentation.cs`(`ClaudePluginBrowser` Codex 분기 — 사용자 범위만, `CodexPluginStrings.FooterNote`·`MarketplaceHelp`, 준비 후에도 상태 줄 표시), `ClaudePluginReader.cs`(`IPluginReader` 공유), `SlashCommands.cs`(`/plugins` 복귀, `UnavailableActions` 비움). WinUI: 공용 `MainWindow.Plugins.cs`가 제공자 이름으로 `CodexPluginReader`를 고르고, `MainWindow.cs`의 `···` 메뉴가 `Codex 플러그인` 항목을 만들며, `MainWindow.Smoke.cs`가 스모크 키 `codexPluginList`로 같은 실제 대화상자를 구동한다 — Claude 목록과 같은 창, 보기 전용. 검사 새 파일 `CodexPluginVerification.cs` 13개가 가짜 `ICliRunner`로 통과(Mac Core.Tests 205개 통과, Windows 전용 1개 건너뜀). **있는 것**: 설치됨·마켓플레이스 두 탭, 검색, 마켓플레이스 거르개, `목록 새로고침`, 진단 출력, CLI 버전 줄. **없는 것**: 설치·제거·활성/비활성 단추, 설치 범위 선택기, `plugin marketplace add`·`upgrade` — 마켓플레이스 기능 묶음이라 23·24행에 보류로 남아 있다. 규칙과 OS 종속 선택은 `docs/windows-plugins.md`. 확인 필요 — OS 이름 치환 문장 1개(`이 Mac의` → `이 PC의`, `CodexPluginStrings.FooterNote`) | 1단계 | Codex 사용 시 플러그인 인식 필요 | 완료 — `ae5625a`·`0cea2a3`·`9ad3c73` (Windows x64·arm64 성공, `2647c4c` 기준. 뒤 커밋은 문서만). `codex plugin …` 검사 13개 포함 Core.Tests 205 통과. `/plugins`가 Codex 팔레트에 돌아와 제외되는 앱 동작이 없다. 기기 미확인 |
| 설정 화면 | `SettingsViews.swift`의 `Form` 본문 (섹션 순서) | `SettingsSections.cs`(Core) — macOS 슬롯 12개를 순서대로 담고 Windows에 있는 것만 보여준다: `화면` → `CLI 업데이트` → `이 PC의 CLI` → `앱 정보`. 테마·완료 알림 스위치는 `화면`, 실행기 목록은 `이 PC의 CLI`로 동작 그대로 이동. WinUI `MainWindow.Settings.cs`는 슬롯별 빌더만 붙인다(`BuilderFor`). 아직 없는 기능(원격 연결·스타일·컴포넌트·모바일 원격·컴패니언·Claude Mods·앱 업데이트)은 표시하지 않는다; CLI 계정은 등록 완료. `settings sections …` 검사 5개 Mac 통과(`SettingsSectionsVerification.cs`); 스모크 키 `settingsSections`; 등록 방법은 `docs/windows-settings-groundwork.md` | 1단계 | P1 기능(계정·업데이트)이 설정 화면 필요 | 완료 — 섹션 등록 토대(`docs/windows-settings-groundwork.md`) 위에 CLI 업데이트·CLI 계정·계정 사용량·앱 업데이트·완료 알림 섹션이 올라갔다. 순서·누락 섹션·스위치 반전/복원은 Mac에서 검증. 기기 미확인 |
| 인터랙티브 터미널 | `LocalTerminalSession.swift`, `LocalTerminalView.swift` (Ghostty/Metal/PTY) | `shell` 실행 창 (요청별 실행, PTY 없음, 상태 비유지) | 터미널 | P1·P2 사이 별도 항목으로 명시 (interview_20260919_235018) | |
| 스타일 엔진 | `Styles/*.swift`, `AppStore+Styles.swift`, `StyleApprovalSheet.swift` | 없음 | 2단계 | P2 합의 (interview_20260919_235018) | |
| 가이드 패널 | `GuidedPanel.swift`, `GuidedActionChip.swift`, `AgentQuestionPanel.swift` | 없음 | 2단계 | P2 합의 (interview_20260919_235018) | |
| 실행 그래프 | `MightyGraph.swift`, `ExecutionGraph.swift`, `MightyGraphView.swift` | 없음 | 2단계 | P2 합의 (interview_20260919_235018) | |
| 컴포넌트 (설치 대시보드) | `Components.swift`, `AppStore+Components.swift`, `ComponentsSettingsView.swift`; docs/components.md | 없음 | 2단계 | 스타일 엔진·플러그인 상태 확인 UI; P2 완성도 | 확인 필요 |
| 워크스페이스 Git 정보 | `WorkspaceGitInfo.swift`, `WorkspaceGitView.swift` | 없음 | 2단계 | 그래프 화면 보조 정보; P2 작업과 연관 | 확인 필요 |
| 컨텍스트 압축 표시 | `ContextCompaction.swift` | 없음 | 2단계 | CLI 이벤트 처리; P2 필기록 완성도 | 확인 필요 |
| 그래프 블록 크기 조정 | `AppStore+GraphResize.swift`, `MightyGraphBlockSize.swift` | 없음 | 2단계 | 실행 그래프 내 UX; P2 작업에 포함 | 확인 필요 |
| 참조 링크 버블 | `ReferenceLinks.swift`, `MightyGraphReferenceBubble.swift` | 없음 | 2단계 | 실행 그래프에서 표시되는 참조 링크 | 확인 필요 |
| 세션 템플릿 | `SessionTemplate.swift` | 없음 | 2단계 | 상태 복구·재개; P2 이후 UX | 확인 필요 |
| 동반 펫 | `CompanionPet.swift`, `CompanionCarousel.swift`, `CompanionBubbleController.swift` | 없음 | 3단계 | P3 합의 (interview_20260919_235018) | |
| 모바일 릴레이 호스트 | `MobileRemoteService.swift`, `RelayChannel.swift`, `MobileDeviceRegistry.swift` | 없음 | 3단계 | P3 합의 (interview_20260919_235018) | |
| 사용자 설문 | `UserQuestionnaire.swift`, `UserQuestionnaireCard.swift`, `QuestionnaireProgress.swift` | 없음 | 3단계 | 온보딩 설문; P3 UX 완성도 | 확인 필요 |
| 한글 폴백 입력 | `HangulComposer.swift`, `HangulFallback.swift`, `InputMethodSymptom.swift` | 해당 없음 (Windows IME 정상) | Windows에 해당 없음 | 명시적 "Never on Windows" (interview_20260919_235018) | |
| 실행 창 레이아웃 | `PaneLayout.swift`, `AppStore+PaneLayouts.swift` | `PaneLayout.cs`, `PaneLayoutVerification.cs` — 구현됨 | 이미 있음 | `PaneLayout.cs` 존재 및 구조 일치 | |
| 에이전트 활동 | `AgentActivity.swift`, `AgentRunTiming.swift` | `AgentActivity.cs`, `AgentRunTiming.cs` — 구현됨 | 이미 있음 | 대응 C# 파일 존재 | |
| 에이전트 필기록 | `AgentTranscriptView.swift`, `AgentTranscriptFormat.swift` | `AgentTranscript.cs` — 구현됨 | 이미 있음 | `AgentTranscript.cs` 존재 | |
| 파일 첨부 | `Attachments.swift`, `ComposerAttachments.swift` | `AttachmentSupport.cs`, `AttachmentInput.cs` — 구현됨 | 이미 있음 | `AttachmentSupport.cs`, `AttachmentInput.cs` 존재 | |
| 원격 워크스페이스 | `Remote/RemoteService.swift`, `TailscaleDiscovery.swift` | `RemoteServer.cs`, `RemoteController.cs`, `RemoteNetwork.cs` — 구현됨 | 이미 있음 | 대응 C# 파일 존재 | |
| 세션 컨텍스트 사용량 | `SessionUsage.swift` | `SessionUsage.cs`; `MainWindow.cs` context 버튼 — 구현됨 | 이미 있음 | `SessionUsage.cs` 및 UI 구현 존재 | |
| 모드 브릿지 | `ModBridge.swift` | `ModBridge.cs` — 구현됨 | 이미 있음 | `ModBridge.cs` 존재 | |
| 프로바이더 카탈로그 | `ProviderService.swift`, `ProviderMark.swift` | `ProviderCatalog.cs` — 구현됨 | 이미 있음 | `ProviderCatalog.cs` 존재 | |
| IME 조합 처리 | `NativeComposerEditor.swift` (macOS AppKit IME) | `MainWindow.cs` `suppressCompositionEnter` 로직 — 구현됨 | 이미 있음 | Enter 억제 코드 `MainWindow.cs:271-279` 존재 | |
| 권한 모드 (모드 선택·자동 허용) | `ToolPermissions.swift`, `ToolPermissionPresentation.swift` | `RunSettings.PermissionMode`; `AutoPermissionVerification.cs` — 구현됨 | 이미 있음 | 권한 모드 열거 및 자동 모드 구현됨. 앱 내 승인 채널은 아래 행으로 분리 | |
| Claude 추가 권한 요청의 앱 내 승인 (실행 창·펫 말풍선) | `ToolPermissions.swift`(`can_use_tool` 처리), `ToolPermissionBar.swift`, `AgentCompanion.swift` | `ToolPermissions.cs`(승인 채널)·`ToolPermissionPresentation.cs`·`ToolPermissionStrings.cs`, WinUI `MainWindow.ToolPermission.cs` — 실행 창의 승인 막대(제목·요약·이유·경로·대기 수, `이번만 허용`/`거부`); `tool permission` 검사 Mac 통과; 스모크 키 `toolPermission`; 설문·펫 말풍선·스타일 자동 허용은 범위 밖(`docs/windows-tool-permissions.md`) | 1단계 | 승인 채널이 없으면 자동 허용 밖의 도구 요청에서 실행이 멈춘다; 핵심 작업 흐름 | 완료 — `374841b`(Windows x64·arm64 성공). Core `0c0db9f`·`5f997ed`, 실행 경로 `68077d9`, 화면 `e30c797`. 기기 미확인 |
| 완료 알림 (시스템 알림) | `AgentCompanion.swift`(`UNUserNotificationCenter`) | `CompletionNotifications.cs`(결정 논리)·`CompletionNotificationStrings.cs`(문구), WinUI `MainWindow.CompletionNotification.cs`(`ICompletionNotifier`·`WindowsAppNotifier`·설정 섹션·스모크); `CompletionNotificationVerification.cs` 7개 검사 Mac 통과; 스모크 키 `completionNotification`; `docs/windows-completion-notification.md` | 1단계 | 시드 롤아웃 순서 3번 — 펫과 같은 완료 신호 기반, 토스트로 구현 | 완료 — `8a2d154`(Windows x64·arm64 성공). Core `3d5437e`, 알림·설정 `95a803a`. CI의 실제 호출은 두 러너 모두 `skipped (IsSupported false)` — 전송·표시는 기기 미확인 |
| 모델 기본값 (제공자·권한 모드별) | `SettingsViews.swift` 모델 기본값 섹션, `SessionPaneView.swift` 권한 모드 메뉴, `MightyGraph.swift` 요청 노드 모델 라벨 | `ModelDefaults.cs`(해석 규칙·CoreValidation·삭제 시 모드 되돌리기·effort 포함 등록 항목), `SettingsSections.cs`(설정 섹션 등록) — 설정 화면에서 Claude·Codex 제공자별 권한 모드별 기본 모델과 직접 등록 이름을 관리; `ModelDefaultsResolution.BuildPaneRequest`가 실행 경로에서 해석 규칙을 적용하고 등록 모델을 RunManager에 전달; 권한 모드 메뉴가 `ModeMenuLabel`로 해석된 모델 이름을 표시; 해석 규칙(직접 선택 > 워크스페이스 > 앱 > CLI 기본)이 Core.Tests에서 증명됨; 그래프 노드 모델 라벨은 Windows에 그래프가 없으므로 구현하지 않음. `model defaults …` 검사 Core.Tests 통과; 규칙은 `docs/model-defaults.md` | 1단계 | v5 첫 항목 (model-defaults-plan.md) | 완료 — `efddcd2`·`8ad0ea1` (설정 섹션), run rule `v4-work` 진행 중. 기기 미확인 — 설정 화면 모델 기본값 섹션·권한 모드 메뉴 모델 이름·직접 등록 이름 등록 UI·직접 등록 이름의 CLI 거부 오류 표시·권한 메뉴 모드별 해석 이름·등록 모델 effort 수준 추론 강도 메뉴 |
| 플러그인 마켓플레이스 (Claude·Codex) | `ClaudePluginService.swift`, `ClaudePluginView.swift`, `CodexPluginService.swift`, `AppStore+Components.swift` | Core 새 파일: `PluginOperations.cs`(설치·마켓플레이스 새로고침 메서드, 인자 목록 조합, 한 번에 하나 게이트, 취소, 종료, `ClaudePluginOperationResult`). Core 새 파일: `PluginMarketplaceVerification.cs`(`plugin marketplace …` 검사 20개 — 인자 목록·범위 검증·결과 문장·바쁨 거절·실행 앱 연결). 고친 Core 파일: `ClaudePlugins.cs`(`InstallAsync`·`RefreshMarketplacesAsync`·`ScopeOptions`·`CanInstall`·`CanRefreshMarketplaces`·`IsMutating`·`CanCancel`·`RequestCancel`·`NamesAllowedChange`), `PluginStrings.cs`(설치·새로고침 작업 문구 macOS 원문). 고친 WinUI 파일: `MainWindow.Plugins.cs`(scope-picker·install-*·refresh-marketplaces·cancel-operation·operation-progress·operation-result 컨트롤, `RunPluginMarketplaceSmoke`·`FakeMarketplaceReader`), `MainWindow.Smoke.cs`(스모크 키 `pluginMarketplace`). 스모크가 가짜 리더로 설치·취소·새로고침을 구동하고 세 macOS 결과 문장을 검증. `PluginOperations`가 실제 앱에서 단 하나의 창 필드로 들어가 설치와 새로고침을 사용자 클릭에서 실행. `plugin marketplace` Core.Tests 검사 20개 포함 225개 통과. 규칙과 OS 종속 선택은 `docs/windows-plugins.md`. **없는 것**: 설치 취소 후 되돌리기(macOS도 없음), 진행 중 다른 창에서 두 번째 설치(바쁨 문장으로 거절함). 기기 미확인 — 실제 claude/codex CLI 설치·마켓플레이스 새로고침은 기기에서 확인 필요 | 2단계 | 플러그인 목록(1단계) 위에 얹는 설치·새로고침 UI | 완료 — `28b41e6`(Core)·`1f2563f`(화면)·`4551361`(스모크)·`21c8876`(실제 앱 연결) (Windows x64·arm64 성공, `3dad584` 기준). `plugin marketplace …` 검사 22개 포함 Core.Tests 227 통과. macOS와 같은 CLI 명령만 쓴다(Claude `plugin install --scope`·`plugin marketplace update`, Codex `plugin add`·`plugin marketplace upgrade`). 제거·켜기·끄기·마켓플레이스 추가는 macOS에 없어 만들지 않았다. 실제 설치는 기기 미확인 |
| 원격 보안 저장소 | `Remote/RemoteKeychain.swift` (macOS Keychain) | `SecretProtector.cs` (DPAPI) — 구현됨 | 이미 있음 | `SecretProtector.cs` 존재 | |
| Windows 빌드·배포 파이프라인 | `scripts/build-macos.sh`, `scripts/install-macos.sh` | `scripts/build-windows.ps1` (기본 빌드만); 서명·MSIX·winget 없음 | 배포 인프라 | v3 이후 코드 서명·패키징·배포 채널 정비 필요 | 확인 필요 |
